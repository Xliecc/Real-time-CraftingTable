# Changelog

## [1.0.0] - 2026

首个正式版本。

### 新增

- 工作台 3D 实时合成预览：材料悬浮在槽位上方，配方成立时结果浮于材料上方并缓慢自转
- 关闭后保留（工作台当容器）：开启后材料归档到 JSON 持久化，重开同一张桌继续合成，可自动恢复
- 大量自定义：悬浮/平摊、波浪/同步/不浮动、朝向跟随/固定、生长动画、渲染距离、大小/间距/高度/速度/幅度，可适配各类材质包
- 联机同步：LAN / 独立服务器下材料状态、工作台朝向、保留记录跨玩家实时同步（装 mod 的玩家之间）
- vanilla 客户端兼容：服务端注册表零自定义条目，原版客户端可直接进服
- 服务端可选：客户端单独安装即可预览（连 vanilla 服务器也可用）；服务端装上后可保留/共享/同步
- Mod Menu + Cloth Config 图形化配置，中英双语本地化
- 附魔台参考：参考 Real-time EnchantmentTable 的渲染与联机架构

### 性能优化

- 漫游早退：无 GUI / 无存储记录时整帧跳过全量遍历（闲置工作台零开销）
- 渲染状态复用：每帧复用 map/set，避免重复分配
- 距离裁剪：超出渲染距离的预览不构建渲染状态
- 广播合并：同一 tick 内同一位置多次编辑只广播一次（tick 末统一 flush）
- 落盘合并：编辑过程只写内存，终态才写盘（关桌/破坏/停服）

### 修复

- 原版客户端被 registry sync 拒绝进服（移除了自定义 BlockEntityType 注册，渲染改用手动路径）
- 服务端未装 mod 时客户端预览不定位（新增客户端本地交互追踪，右键工作台即定位渲染）
